# Staging Environment Configuration
# This file is used for staging/preview deployments

# Database Configuration - Staging Supabase Project
# Note: You should create a separate Supabase project for staging
# Replace these with your staging database credentials
DATABASE_URL="postgresql://postgres.staging_project_id:staging_password@aws-0-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# For migrations (use this with Prisma CLI)
DIRECT_URL="postgresql://postgres.staging_project_id:staging_password@aws-0-ap-south-1.pooler.supabase.com:5432/postgres"

# Legacy database config (kept for compatibility)
DB_HOST=aws-0-ap-south-1.pooler.supabase.com
DB_PORT=5432
DB_USERNAME=postgres.staging_project_id
DB_PASSWORD=staging_password
DB_NAME=postgres

# Supabase Configuration - Staging Project
SUPABASE_URL=https://staging_project_id.supabase.co
SUPABASE_ANON_KEY=your_staging_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_staging_service_role_key_here

# JWT Configuration
JWT_SECRET=cosmic_love_jwt_secret_key_staging_2024
JWT_EXPIRES_IN=7d

# Application Configuration
PORT=3000
NODE_ENV=staging

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_DEST=./uploads

# Real-time Configuration
SOCKET_PORT=3001

# Staging-specific configurations
DEBUG_MODE=true
LOG_LEVEL=debug
ENABLE_SWAGGER=true

# CORS Configuration for staging
CORS_ORIGIN=https://staging-frontend-url.netlify.app

# Rate limiting (more lenient for staging)
THROTTLE_TTL=60
THROTTLE_LIMIT=100
